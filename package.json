{
  "name": "root-build",
  "private": true,
  "engines": { "node": ">=20 <23" },         
  "scripts": {
    "heroku-postbuild": "npm ci --prefix frontend --production=false && npm run build --prefix frontend"
  }
}
